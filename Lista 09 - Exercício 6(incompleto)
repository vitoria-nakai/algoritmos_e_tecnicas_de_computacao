/*
Faça uma função que aloque uma matriz com as linhas e colunas desejadas pelo usuário.
*/

#include<stdio.h>
#include<stdlib.h>

int **aloca(int i, int j){
    int **matriz = (int**)malloc
    for(int x = 0; x < i; x++){
        for(int y = 0; y < j; y++){
            int matriz[i][j];
            printf("Qual é o valor do elemento da linha %d e coluna %d?", i, j);
            scanf("%d", &matriz[i][j]);
}

int main(){
    int i, j;
    printf("Digite a quantidade de linhas e colunas que deseja na matriz(ex: 1 2:");
    scanf("%d %d", &i, &j);
    for(int x = 0; x < i; x++){
        for(int y = 0; y < j; y++){
            printf("Qual é o valor do elemento da linha %d e coluna %d?", i, j);
            scanf("%d", )
        }
    }
    getchar();
    return 0;
}
